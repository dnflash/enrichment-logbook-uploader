# Enrichment Logbook Uploader

## How to run

- Run with Python 3.7+

### On Linux


cd to the project directory, then run:

```
python3 -m venv venv
. venv/bin/activate
python3 -m pip install -r requirements.txt
python3 main.py
```